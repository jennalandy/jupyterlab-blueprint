let { MessageBarButton, Link, Label, MessageBar, MessageBarType, Fabric } = window.Fabric;

const log = (text: string): (() => void) => (): void => console.log(text);

ReactDOM.render(<MessageBarBasicExample />, document.getElementById('content'));
